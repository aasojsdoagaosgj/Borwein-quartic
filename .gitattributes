# The SHA-256 manifests in this repository are taken over the exact bytes
# below. Disable end-of-line conversion so the checks pass on every platform,
# including a default Windows clone (core.autocrlf=true).
* -text
