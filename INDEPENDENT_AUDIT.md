# Berkovich–Dhar 法3・4乗：large-n 証明稿の監査報告

*This document is written in Japanese.*

監査日：2026-09-17。対象：本リポジトリの large-n 証明稿と添付の計算物、および監査依頼文書。

**総合判定：成立と判断。** 今回確認した範囲では、存在型の large-n 定理を妨げる数学的誤り・未解決の証明不足は見つからなかった。結論を維持するために必要な修正はない。後述する改善提案は、説明・文献指定・診断コードの検査条件を明確にするためのもの。

この判定は、証明の再導出、原典との照合、証明書コードの読解と再実行に基づく AI 監査である。人間による査読・形式検証・新規性の認定を意味しない。原稿や既存の自己点検の「証明済み」という評価は根拠にせず、compact、端点、無限積、コードを分担して検証した。既存の `quartic/AUDIT.md` は主要な独立判断の後に照合した。

## 1. 成立と判断した範囲

```math
P_n(q)=\prod_{r=1}^{n}(1-q^{3r-2})(1-q^{3r-1}),\qquad
P_n(q)^4=\sum_{j=0}^{12n^2}c_j(n)q^j.
```

1. ある整数 $N_0$ が存在し、すべての $n\ge N_0$ で、$c_{3k+2}(n)$ は正から負へ一度だけ転換する。零係数は、存在してもその転換点の高々一個。
2. $c_{3k}(n)\ge0$ はすべての $n\ge1$ で成立する。これは引用された第二 Borwein 定理からの代数的帰結。
3. 最後の非負係数の添字を $k_n$ とすれば、

```math
k_n=\alpha_*n^2+\beta_*n+O(1)
```

が成立する。$\alpha_*$ の式と有理区間証明書、$\beta_*$ の厳密な定義式は整合している。

具体的な $N_0$ は得ていない。したがって、これを「全正整数 $n$ に対する予想の完全解決」とは判定しない。一方、依頼された存在型の定理では、閾値を数値化しないこと自体は欠陥ではない。

## 2. 指摘と重大度

| 分類 | 箇所 | 監査結果・推奨事項 | 主定理への影響 |
|---|---|---|---|
| 致命的誤り | 証明全体 | 発見なし | — |
| 結論を出すために補う必要がある証明不足 | C6、C8、E2–E8、初期係数と全域接続 | 発見なし | — |
| 説明の明確化 | `COMPACT_PROOF.md` §3、C6 | 位相を七次、振幅を五次・三次・一次まで展開することを明記すると追いやすい | 現状の議論から導ける。修正必須ではない |
| 説明の明確化 | `ENDPOINT_PROOF.md` §2、E3–E4 | 複素垂直線分でも同じ導関数上界が使えることを一文加えるとよい | E3 が既に保証している |
| 文献指定・記述 | `INFINITE_SIGNS.md` の Wang 論文参照と末尾の表への注意 | URL を v3 に固定し、Table 3 の不整合は PDF p.24 自体にあると記述できる | 証明はその表を用いないので影響なし |
| 診断の検査条件 | `diagnostic.py` | 零係数の個数・位置は一覧に記録されるが、その条件をすべて assert してはいない。検査意図をコードにも明記するとよい | 実際の n≤65 の剰余2の零係数は全行で空。解析証明には影響なし |
| 保存済み診断の依存関係 | `saddle_diagnostic.py` と `verify.py` | import している `diagnostic.py` の依存ハッシュも結果に記録すると、将来の保存結果のみの検証が強くなる | 今回は全ファイルの梱包ハッシュ一致と全再実行を実施したので影響なし |
| 梱包上の参照 | `README.md` 末尾 | `results/usage_checkpoint.json` への参照先は今回の ZIP に含まれない | 利用量の記録であり、証明の依存資料ではない |

「一般的に厳密化が必要」という理由で判定を保留する必要はないと判断した。問題になりやすい一様性と格子点上の転換について、原稿には必要な評価が書かれている。

## 3. Compact 展開の監査

### 3.1 Euler–Maclaurin、分枝、C1

$\omega=e^{2\pi i/3}$ とし、$f_b(u,z)=\log(1-\omega^b e^{-zu})$ とおく。実区間 $[0,1]\times[0,7]$ 上では対数内部は零にならない。したがって、十分小さい共通の複素近傍で分枝を選べる。微分の一様有界性はこの固定近傍のコンパクト性から得られる。

標本点は $(k+b/3)/n$。独立に評価した Bernoulli 多項式は

```math
B_1(1/3)=-1/6,\quad B_1(2/3)=1/6,
```
```math
B_2(1/3)=B_2(2/3)=-1/18,\quad
B_3(1/3)=-B_3(2/3)=1/27.
```

これを第四乗に対応する係数4と合わせると、C1 の $h,b,d$ が再現される。特に

```math
d(z)=-\frac{2i\sqrt3\,z^2x(1-x^2)}{81(1+x+x^2)^2},\qquad x=e^{-z},
```

の符号も一致する。反対側の根では $h,d$ の符号が反転し、$b$ は共通となる。有限次数の Euler–Maclaurin 剰余は共通の複素近傍で一様に評価でき、さらに小さい近傍で Cauchy 評価により必要な導関数も一様に扱える。

### 3.2 主振幅と転換点、C2

$H=4R$、$\alpha=-H'$ とすると、$H''>0$ は確率変数 $J_{\tau u}\in\{0,1,2\}$ の分散表示から従う。よって鞍点は一意。

```math
\Psi_2(\tau)=2\cos(\phi(\tau)+2\pi/3)
```

の零点条件は $\phi(\tau_*)=-\pi/6$。これを原稿の $\phi$ に代入すると

```math
\arctan\frac{\sqrt3 x_*}{2+x_*}=\frac\pi{24},\qquad
x_*=\frac{2\tan(\pi/24)}{\sqrt3-\tan(\pi/24)}.
```

従って C2 の式は正しい。$\phi'<0$ なので $\Psi_2'(\tau_*)>0$。他の主振幅は対象区間で $\Psi_0>0$、$\Psi_1<0$ であり、必要な符号余裕を持つ。

### 3.3 外円弧、C3–C4

[Wang–Krattenthaler, arXiv:2201.12415v1](https://arxiv.org/pdf/2201.12415v1) の pp.24–25、Lemmas 9.2・9.4 を確認した。原稿の C3・C4 は式と適用条件が一致する。この二つは $P_n$ 自体の絶対値比の評価であり、第四乗に使える。べきが1・2・3に制限された Lemma 9.5 の無断適用はない。

$r=e^{-\tau/(3n)}$、$0\le\tau\le7$ では、二つの非自明三乗根から幅 $\eta/(3n)$ の円弧を除いた部分で、原稿どおり $D\ge cn-C$ を得る。1に近い部分を C4、それ以外を C3 で覆う分割は全円周を覆う。$r=1$ は極限で扱え、有限積の零点における絶対値上界にも支障はない。

### 3.4 C6 の O(n^-3) 剰余

$\lambda=n^{-1/2}$、$y=\lambda v$ と置くと、線形項は鞍点方程式で消え、二次項は $-gv^2/2$。$g=H''$ はコンパクト区間で正の下限を持つ。

原稿の「$n^{-1/2}$ の五次まで展開」を具体化すると、次で十分である。

- 位相の $H(\tau-i\lambda v)$ は七次まで展開する。指数内の残りは $O(\lambda^6|v|^8)$。
- 振幅を $f_0+\lambda^2f_1+\lambda^4f_2+O(\lambda^6)$ と書く。
- $f_0,f_1,f_2$ の変数 $\lambda v$ に関する Taylor 展開は、それぞれ五次・三次・一次までで足りる。

ここで

```math
f_0=\Psi_a,\quad f_1=b\Psi_a,\quad
f_2=\frac{b^2}{2}\Psi_a+d(\omega^{-a}e^h-\omega^ae^{-h}).
```

$|v|\le n^{1/20}$ では $\lambda|v|^3=o(1)$。従って指数関数の展開も含め、Gaussian 重みを掛けた残りを

```math
Cn^{-3}(1+|v|^K)e^{-cv^2}
```

で一様に支配できる。$\lambda$ の次数と $v$ の偶奇が一致するため、一次・三次・五次項の対称積分は消える。その外側は Gaussian 尾部、さらに外の円周は先の指数減衰で制御される。

よって C6 の一様な値剰余 $O(n^{-3})$ は、書かれた議論から導ける。根拠なく誤差を微分する必要はない。

### 3.5 C7 と C7a

Gaussian の二・四・六次モーメントから C7 を再導出して一致した。C7a の有限和の次数条件

```math
p+e_3+2e_4+3e_5+4e_6=4
```

は、正確に $n^{-2}$ の条件である。全ての項で

```math
D=p+3e_3+4e_4+5e_5+6e_6
```

は偶数となり、位相因子は $(-i)^D=(-1)^{D/2}$。$\mathcal S_1(b\Psi_a)$ により $b$ の微分も含まれている。七階の位相微分が C7a に現れないのは、その寄与が五次の奇項として積分で消えるためである。

### 3.6 C8：格子点での一回だけの転換

同じ剰余類で次数が3増えると

```math
\Delta\alpha=\frac1{n^2},\qquad
\Delta\tau=-\frac1{n^2H''(\tau)}+O(n^{-4}).
```

正規化係数 $u_{n,j}=c_j/\mathcal B_{n,j}$ の差を取ると、$\Psi_2$ の差が負の $n^{-2}$ 項、$A_1/n$ の差が $O(n^{-3})$、$A_2/n^2$ の差が $O(n^{-4})$。二つの値剰余の差も $O(n^{-3})$ のままである。

```math
u_{n,j+3}-u_{n,j}
=-\frac{\Psi_2'(\tau)}{n^2H''(\tau)}+O(n^{-3}).
```

零点の固定近傍 $J$ では $\Psi_2'/H''$ に正の下限がある。従って十分大きいすべての $n$ で、この差は一様に負。各 $\mathcal B_{n,j}>0$ なので係数の符号を保存し、厳密な減少から再反転と二個以上の零係数を排除できる。近傍外の符号は C6 の非零主項から確定する。

### 3.7 C9：線形補正

$\tau=\tau_*+s/n$ と置くと、一次補正までの零点条件は

```math
s=-\frac{A_{1,2}(\tau_*)}{\Psi_2'(\tau_*)}.
```

鞍点方程式を戻すことで

```math
\beta_*=
\frac{H''(\tau_*)A_{1,2}(\tau_*)}{\Psi_2'(\tau_*)}
=-\frac{\Psi_2''(\tau_*)}{2\Psi_2'(\tau_*)}
+\frac{H'''(\tau_*)}{2H''(\tau_*)}.
```

C9 と一致する。候補位置での残り $O(n^{-2})$ と、格子間差の大きさの下界 $cn^{-2}$ から、実際の転換位置の誤差は格子点の個数で $O(1)$。$j=3k+2$ の定数差もこの項に収まる。

## 4. 端点領域の監査

### 4.1 Eta 変換、E1–E2

[DLMF 23.18.5–7](https://dlmf.nist.gov/23.18#E5) の変換公式と乗数を照合した。

```math
\sigma=\frac13+\frac{iw}{2\pi},\qquad
\sigma'=\frac{\sigma}{1-3\sigma}
=-\frac13+\frac{2\pi i}{9w}
```

と置く。$\eta(\sigma)$ と $\eta(3\sigma)$ への変換では、平方根因子が比で消え、eta 比の乗数は $e^{-i\pi/6}$。さらに $P_\infty$ と eta 比をつなぐ因子を含めると

```math
\exp\left(\frac{\pi i}{6}(\sigma-\sigma')-\frac{\pi i}{6}\right)
=\exp\left(\frac{\pi^2}{27w}-\frac w{12}-\frac{\pi i}{18}\right).
```

第四乗して E1 の位相 $-2\pi/9$ を得る。根の選択に曖昧さはない。反対の根について、複素 $z$ をそのまま共役する誤りもなく、二つの正則な恒等式として処理している。

有限積への尾部の対数は、実部に対応する $K_n$ と位相 $V_n$ の級数に正確に分かれる。$|y|\le3\tau/2$ では双対積の引数は

```math
|t(\tau-iy)|\le e^{-16a_0N/13},\qquad N=n/\tau,
```

となり、E2 の誤差は $\tau$ に依存しない定数で指数的に小さい。

### 4.2 導関数・減衰・振幅、E3–E6

E3 の級数上界は、絶対値を取り、内側の和を積分と比較して再導出できる。比較する関数 $(1+s)^p e^{-k\tau s}$ は $p\le4,\tau\ge6$ で減少するため、不等号の向きは正しい。この上界は $\operatorname{Im}z$ に依存しない。

```math
|K_n''(\tau-iy)|
\le\frac{12n}{\tau^3}(\tau^2+2\tau+2)
\frac{e^{-\tau}}{1-e^{-\tau}}
\le\frac{600}{399}\frac n{\tau^3}.
```

実軸の $K_n''<0$ と合わせ、E4 の

```math
\frac{20}{3}\frac n{\tau^3}<g<\frac{28}{3}\frac n{\tau^3},\qquad
|L_n'''|<39n/\tau^4
```

は妥当。複素線分で局所 Taylor 評価に使う場合も、E3 が必要な上界を保証する。

E5 は逆数項の実部を正確に計算し、尾部だけを二階導関数で押さえている。$|u|\le3/2$ で

```math
\Re\{L_n(\tau-iy)-L_n(\tau)\}
\le-\frac8{15}Nu^2,\qquad u=y/\tau
```

となる定数の余裕も確認した。局所的な二次近似を円弧全体へ無断で拡張してはいない。

$|1+2\cosh s|\ge1$ の評価、複素領域での振幅有界性、実軸での交代級数による $0<V_n<1/100$ も成立する。従って

```math
T_0>1,\qquad T_1<-1,\qquad T_2>1/4
```

の一様な非零余裕を確保できる。

### 4.3 全円周と係数積分の正規化、E7

二つの主円弧を除いた部分は、同じ文献の C3・C4 によって $Ce^{-cN}|F_n(\omega r)|$ で制御できる。ここで $r=e^{-1/(3N)}$、$1-r^{3n}=1-e^{-\tau}\ge1-e^{-6}$ であり、$\tau$ が増大しても定数は独立である。

係数積分を正規化した後に増える因子は

```math
n\sqrt g=O\left(n\sqrt{n/\tau^3}\right)=O(N^{3/2}).
```

従って補円弧の寄与は $O(N^{3/2}e^{-cN})$。独立な $n$ の増大が残り、指数減衰を壊すという問題は生じない。

主円弧では $y=\tau v/\sqrt N$ により、位相の三次剰余と振幅の変化はともに Gaussian 積分後 $O(N^{-1/2})$。正規化を含めて E7 は成立すると判断した。

### 4.4 鞍点範囲と E8

$-L_n'$ は $[6,\infty)$ で厳密に減少し、極限は $1/(9n)$。対象範囲の鞍点は一意に存在する。$j\ge3n$ と $K_n'>0$ により

```math
\tau^2<5n,\qquad N>\sqrt{n/5}.
```

従って E7 の誤差は、対象全係数で一様に $O(n^{-1/4})$ に落とせる。固定 $\tau$ の漸近式を増大する $\tau$ に流用しているわけではない。

## 5. 無限積と初期係数

[Liuquan Wang, arXiv:2108.03932v3](https://arxiv.org/pdf/2108.03932v3) の pp.5–6、Theorem 1.1 と関連式を原文および PDF 画像で確認した。$t=3,m=4,\mu=1/3,A=1,M=\sqrt2/3$ の代入は正しい。Dedekind 和と $I_{-1}=I_1$ を用いると、原稿 I2 の位相・Bessel 因子・定数が一致する。

第一誤差項の係数は $(\pi^7/24)^{1/4}<4$。残る項を $6e^{11}$ と $54e^8$ で押さえ、合計を $10^7$ 未満とする上界も正しい。

[DLMF 10.32.2](https://dlmf.nist.gov/10.32#E2) の $\nu=1$ を使った積分を、長さ $1/(2X)$ の区間に制限すれば

```math
I_1(X)\ge\frac{e^{X-1}}{2\pi\sqrt{2X}}>\frac{e^X}{25\sqrt X}
```

を得る。原稿の主項下界 $e^X/(200d^{3/4})$ は安全側である。

$d\ge300$ における I3 の単調性と境界代入を独立に確認した。正の Taylor 和による $e^{17}$ の下界を使った誤差比の上界は

```math
0.01096419558400961\ldots<1/50.
```

$j\ge301$ なら $d=j-1/3\ge300$ なので、この範囲で符号は厳密に確定する。

$0\le j\le300$ は、添付の因子乗算コードを再実行したほか、独立な対数微分の漸化式

```math
j b_j=-4\sum_{k=1}^{j}
\left(\sum_{\substack{d\mid k\\3\nmid d}}d\right)b_{j-k},\qquad b_0=1
```

によって全301係数を再計算した。各段階で整数への割り切れも確認し、配列全体が一致した。符号違反はなく、無限積の全係数の符号 $+,-,+$ が得られる。

有限積から最初に欠ける因子の次数は $3n+1$。従って $0\le j\le3n$ で無限積と有限積の係数は一致し、初期範囲の境界に取り落としはない。

## 6. 全係数の被覆と単一の N0

初期範囲 $j\le3n$、端点範囲 $3n\le j\le j_e(n)$、compact 範囲 $3n^2\alpha(7)\le j\le6n^2$ が接続される。

```math
\frac{j_e(n)}{3n^2}=\alpha(6)+O(n^{-2}),\qquad
\alpha(6)-\alpha(7)>0.
```

したがって、端点範囲と compact 範囲には $n^2$ の大きさの重なりがあり、整数添字の丸めによる空白も生じない。

単一の $N_0$ の存在は、具体的には次のように確認できる。

- C8 で $\delta=\min_J\Psi_2'/H''>0$ とし、差の剰余を $C/n^3$ で押さえる。$n>2C/\delta$ なら全ての対象差が負。
- 転換近傍外では主振幅の最小絶対値が正なので、一様な C6 の誤差をその半分未満にする共通の閾値を取れる。
- E7 の誤差を $C_E/\sqrt N$ で押さえると、E8 から $C_E(5/n)^{1/4}$。例えばこれを $1/8$ 未満にすれば E6 の全符号が保たれる。
- 端点と compact の重なりには正の極限幅があるため、重なりが有効になる共通の閾値がある。

必要な閾値は有限個で、いずれも係数添字に依存しない。最大を取れば要求された $N_0$ を得る。この点に量化の逆転はない。

全次数は $12n^2$、相反性は $c_j=c_{12n^2-j}$。上半分の剰余2は下半分の剰余1へ移り、厳密に負。従って下半分で得た転換が、多項式全体でも唯一の転換となる。

剰余0は [Wang–Krattenthaler の Theorem 10.3](https://arxiv.org/pdf/2201.12415v1) を用いて、第二 Borwein 分解の平方から $A_n(Q)^2+2QB_n(Q)C_n(Q)$ を抽出すれば全 $n$ で非負。未証明の三乗版を使う依存関係はない。

## 7. 定数と文献中の数値の監査

`certify_limit.py` では、区間の四則演算、整数平方根の上下界、Machin 公式の交代級数余り、対数と dilogarithm の正項級数の余りを確認した。区間評価に浮動小数丸めは入っていない。

```math
\tan(\pi/24)=\sqrt6+\sqrt2-\sqrt3-2
```

と $R$ の dilogarithm 表示も一致する。再実行で得た厳密区間は

```math
\boxed{0.74908009478851072443256
<\alpha_*<
0.74908009478851072443257}.
```

さらに、$R$ の数値微分とは別に、$J_{\tau u}$ のモーメント・分散・三次中心モーメントを積分する90桁の数値診断を行い、

```math
\alpha_*=0.7490800947885107244325641805684303556\ldots,
```
```math
\beta_*=0.08814225705231413717412680292112276824\ldots
```

を再現した。この独立数値評価自体は区間証明ではない。$\alpha_*$ の厳密な桁保証は先の有理区間コードが担う。$\beta_*$ の小数は診断値だが、C9 の式と漸近展開の数学的結論は成立する。

式の値が正しいことと、その値が係数転換位置を表すことは別途確認した。後者は C6–C9 と全域接続から従う。

[Berkovich–Dhar, arXiv:2407.13788v3, Conjecture 2.1](https://arxiv.org/html/2407.13788v3#S2) には、第四乗の極限値として実際に $0.750\ldots$、有限例として $k_{65}=3170$ が記載されている。有限例は再現されるが、今回導かれる極限は上記の $0.749080\ldots$。両者を区別する原稿の指摘は妥当。

Wang v3 の PDF p.24、Table 3 も実際に画像で確認した。第四乗の行は正の剰余集合を $\{0\}$、負の集合を $\{1,2\}$ と印刷している。これはテキスト抽出の事故ではなく、原典の表そのものの不整合である。本文公式と今回の直接導出による正しい集合は $\{0,2\}$ と $\{1\}$。$b_2=2$ からも不一致を確認できる。今回の証明がこの表の行に依存していないことも確認した。[原典 v3](https://arxiv.org/pdf/2108.03932v3)

## 8. 実行した計算と限界

元の ZIP と展開物は書き換えず、再実行用コピーで検証した。`mpmath==1.3.0` を使用し、最適化による assert 無効化は行っていない。

| 実行・確認 | 結果 | 保証する範囲 |
|---|---|---|
| `python verify_bundle.py` | 24対象ファイルの SHA256 が一致 | 梱包内容の整合性 |
| `certify_infinite.py` | passed | 301係数と、文書の初期範囲証明に使う有理数の余裕 |
| `certify_endpoint_constants.py` | passed | E4–E8 に使うスカラー不等式 |
| `certify_limit.py` | passed | 定義式の $\alpha_*$ の有理区間 |
| `diagnostic.py` | passed | $1\le n\le65$ の下半分の整数係数の符号・転換検査と数値診断 |
| `saddle_diagnostic.py` | diagnostic_completed | $n=20,40,80,160$ の各4係数、および eta 恒等式の数値診断 |
| `python -B quartic/verify.py --replay` | 全5ジョブ成功 | 上記の再実行と結果・ソース対応の整合性 |

保存済みの5結果ファイルと再実行結果は、実行時間のフィールドを除いて一致した。ZIP外の添付依頼文も ZIP 内の依頼文と一致した。

$n\le65$ の剰余2に対する零係数一覧は全65行で空。$n=65$ は最後の正係数が $k=3170$、最初の負係数が $3171$。$n=160$ の局所検査では $k=19190$ が正、$19191$ が負。後者は全係数検査とは区別する。

さらに、供給コードの `multiply()` を使わず、各因子を

```math
(1-q^r)^4=1-4q^r+6q^{2r}-4q^{3r}+q^{4r}
```

と直接展開する別方式でも整数計算した。全 $1\le n\le65$ の下半分で、剰余0は厳密に正、剰余1は厳密に負、剰余2は零なしの一回転換を確認した。三つの剰余類を通じて零係数はなかった。$n=160$ についても局所4点 $k=19188,19189,19190,19191$ の符号をこの別方式で再現した。

第二補正後の残差を $n^3$ 倍した診断値は、対象標本でおよそ0.0387〜0.0406であり、C6 と整合した。ただし、この標本の一致から一様な誤差評価を推論したわけではない。一様性は第3節・第4節で監査した解析によって保証される。

## 9. 修正提案と未確認事項

主定理を救うための差し替え式・新しい補題は不要と判断した。文章を補うなら、次の程度で十分である。

1. C6 の導出に、第3.4節の「位相七次、振幅五次・三次・一次」の説明を加える。
2. E4 の後に「E3 は虚部に依存しないため、局所 Taylor 展開の複素線分でも同じ導関数上界を用いる」と付記する。
3. Wang の参照 URL を v3 に固定し、Table 3 については PDF画像で確認された不整合と記す。
4. 診断コードにも零係数の数・位置の検査を明記する。現状の計算結果と解析証明を変更するものではない。
5. `saddle_diagnostic.py` の出力に `diagnostic.py` の依存ハッシュを追加する。保存済み結果だけを検査する将来の運用を改善する提案であり、今回の再実行結果には影響しない。
6. README の未同梱の利用量記録への参照は、削除するか今回の梱包外だと明記する。

依頼に挙がった外部の主要依存関係は全て参照できた。Wang–Krattenthaler の引用定理・補題そのものを全て再証明したわけではなく、公表された結果として用いている。必要な式と適用条件は照合した。

今回実施していないのは、数値的な $N_0$ の算出、全 $n$ の確認、形式化、網羅的な新規性調査である。これらは今回の存在型定理の監査範囲とは区別される。原稿・コードの修正、外部連絡・公開は行っていない。

対象 ZIP の SHA256：

```text
af27ffd1fee37aa6db9b414061606131fcea2533b49a1a26d769472ea8c06a23
```

対象依頼文の SHA256：

```text
e94cbcfa8aaca0f00b17c42d7e636ee8318bcf713c78275560ee5af78d53de2e
```
